# 注释语言政策

所有注释必须使用**中文**，包括：
- godoc 注释
- 场景注释（测试函数前的前置条件/预期结果）
- section 分隔注释（如 `// ==== ... ====`）
- 行内注释

**例外**：函数名、类型名、sentinel error 名、API 名称等代码标识符可保留英文原样。

发现已有英文注释时，必须改写为中文。已有准确中文注释的跳过。注释过时或描述不准确的，更新。

## 不修改逻辑

annotate 类 skill 只添加/更新注释，**不修改任何业务逻辑或测试逻辑代码**。
